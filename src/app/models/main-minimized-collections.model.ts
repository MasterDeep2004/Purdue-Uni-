import { QueryExternalModel } from "./query-external.model";
import { MainMinimizedExternal } from "./main-minimized.model";


export interface MainMinimizedExternalCollection extends QueryExternalModel<MainMinimizedExternal> {
}
